================================================================================
SEGUIMIENTO SMS - CONTEXTO COMPLETO DEL PROYECTO
(Resumen para retomar trabajo en una sesión nueva. Leer completo.)
================================================================================
RUTA DEL PROYECTO: C:\Users\PERUVIAN FIT\Desktop\SEGUIMIENTOSMS
ÚLTIMA ACTUALIZACIÓN: 2026-09-03
================================================================================

--------------------------------------------------------------------------------
1. QUÉ ES ESTE PROYECTO (OBJETIVO)
--------------------------------------------------------------------------------
Aplicación web (dashboar) de "Seguimiento de producción de prendas" para un centro
de producción (SMS = Muestra de Venta). Permite:
  - Importar un archivo Excel de seguimiento.
  - Editar el estado de cada prenda y sus observaciones.
  - Ver un dashboard en vivo con KPIs y gráficos.
  - Exportar reportes en Excel y PDF.
  - Trabajar por módulos (Costura / Acabados) marcando prendas con check y guardar.

Tecnología: Streamlit (Python). Persistencia: SQLite. Despliegue: red local LAN.

DATOS DE NEGOCIO:
  - El "Style" es el código de la prenda, pero NO es único: se repite una vez por
    cada "Color Cuerpo". La clave real de cada registro es STYLE + COLOR.
  - El PO actual es "SMS-01432". El prefijo "SMS" indica el cliente/tipo:
      SMS = Muestra de Venta. (No es PRODUCCION en este caso).
  - Datos: 76 estilos, 175 variantes, 54 estilos multi-color, PO único.

--------------------------------------------------------------------------------
2. ESTRUCTURA DE ARCHIVOS
--------------------------------------------------------------------------------
app.py                 -> App Streamlit PRINCIPAL (todo el UI + CSS). ~997 líneas.
datos.py               -> Extracción de datos del Excel (headers dinámicos, grupos Style+Color).
db.py                  -> Capa de persistencia SQLite.
reporte.py             -> Exportación Excel/PDF (usado por la app).
generar_reporte.py     -> Script standalone que genera REPORTE SMS-SUMMER27.xlsx.
generar_pdf.py         -> Script standalone que genera REPORTE SMS-SUMMER27.pdf.
MISHA SMS 1 SE.xlsx    -> Archivo Excel fuente. Hoja usada: STATUS SMS-SUMMER27.
seguimiento.db         -> Base SQLite (se crea con los datos importados).
iniciar_panel.bat      -> Launcher de red para Windows.
requirements.txt       -> Dependencias.
.streamlit/config.toml -> Configuración del tema + servidor.
CONTEXTO_PROYECTO.txt  -> ESTE ARCHIVO (guía de sesión).

--------------------------------------------------------------------------------
3. CÓMO LEVANTAR / VER LA APP
--------------------------------------------------------------------------------
- Ejecutar en la máquina servidor (Windows):
      iniciar_panel.bat
  o manualmente:
      python -m streamlit run app.py --server.address 0.0.0.0 --server.port 8501

- URLs:
      Editor (con todas las pestañas):  http://IP_DE_ESTE_PC:8501
      Solo lectura (solo dashboard):     http://IP_DE_ESTE_PC:8501/?solo=1
      Otras máquinas en la red usan la IP de este PC (o seguimiento.local).

- El dashboard se auto-refresca cada 10 segundos (st.fragment run_every=10).

--------------------------------------------------------------------------------
4. MODOS DE ACCESO (solo lectura vs editor)
--------------------------------------------------------------------------------
- SOLO = st.query_params.get('solo','0') == '1'  (app.py línea ~576)
- Si ?solo=1 -> SOLO VER el dashboard y se detiene con st.stop().
- Si no -> se muestran las pestañas: Importar, Seguimiento, Dashboard, Exportar,
  COSTURA, ACABADOS.
- Diseño: UN SOLO editor (el usuario/esta máquina). Los demás son lectores vía ?solo=1.

--------------------------------------------------------------------------------
5. MODOS RESPONDE A LA PREGUNTA DE CONCURRENCIA
--------------------------------------------------------------------------------
- Costura y Acabados escriben en la MISMA tabla SQLite, pero en registros distintos.
- SQLite serializa escrituras (un solo escritor) => no hay corrupción de datos.
- Si DIFERENTES variantes: no interfieren.
- Si LA MISMA variante: la última acción gana (queda el último estado).
- Los módulos Costura/Acabados SOLO se ven en modo editor (no en ?solo=1).
- En la práctica solo hay un editor, así que no hay conflicto real.

--------------------------------------------------------------------------------
6. ESTRUCTURA DE LAS PESTAÑAS DE app.py
--------------------------------------------------------------------------------
Tab 1  Importar    -> sube Excel, elige hoja, importa. (type='primary')
Tab 2  Seguimiento -> edita estado y observaciones de cada variante.
Tab 3  Dashboard   -> KPIs + gráficos, se auto-refresca.
Tab 4  Exportar    -> botones Excel y PDF.
Tab 5  Costura     -> render_modulo('costura')  -> estado EN COSTURA.
Tab 6  Acabados    -> render_modulo('acabados') -> estado EN ACABADOS.

CREACIÓN DE TABS (app.py ~línea 494):
  tab_import, tab_seg, tab_dash, tab_export, tab_costura, tab_acabados =
      st.tabs(['Importar','Seguimiento','Dashboard','Exportar','Costura','Acabados'])

--------------------------------------------------------------------------------
7. FUNCIONES CLAVE DE app.py
--------------------------------------------------------------------------------
inyectar_css()        -> Inyecta todo el CSS (glassmorphism oscuro + responsive).
render_dashboard()    -> @st.fragment(run_every=10). KPIs, gráficos Plotly, tabla resumen por estado.
titulo_dashboard()    -> genera 'Dashboard de avance (Muestra de venta · PO SMS-01432)'.
render_modulo(modo)   -> UI del módulo Costura/Acabados con buscador + checkboxes + guardar.
fmt_fecha(f)          -> formatea fechas a dd-mm-aaaa.

TÍTULO DEL DASHBOARD (función titulo_dashboard, app.py ~línea 448):
  base = 'Dashboard de avance'
  PO = primer po de los datos. Cliente = prefijo del PO (antes del guión).
  prefijo 'SMS' => 'Muestra de venta'. Otro prefijo => ese texto literal.
  Resultado actual: 'Dashboard de avance (Muestra de venta · PO SMS-01432)'
  Se usa en st.header(...) en el modo ?solo=1 y en la pestaña Dashboard.

KPIs del dashboard (render_dashboard, app.py ~línea 648):
  k1 Estilos, k2 Variantes, k3 Total PEDIDO, k4 Total PROGRAMADO,
  k5 'Avance' con valor texto 'En Progreso'  (se puso texto en vez de '%').
  NOTA: la métrica k5 se llama 'Avance' y muestra literalmente 'En Progreso'.

TABLA RESUMEN POR ESTADO (render_dashboard, app.py ~línea 697):
  - Tabla HTML con estilo glassmorphism (.sms-resumen) que muestra por cada estado:
      Estado | Pedido | Programado
  - Estados mostrados: TELA X ING 3-SET, EN LAVANDERÍA, EN CORTE, EN COSTURA,
    EN ACABADOS, ENCAJADO (solo los que tienen datos).
  - Fila final: TOTAL con la suma de Pedido y Programado.
  - Cada estado tiene un punto de color (misma paleta CHART_COLORS que los gráficos).
  - Se renderiza con st.markdown (HTML) en vez de st.dataframe para control total del estilo.
  - Números formateados con separadores de miles (f'{:,}').
  - Se auto-refresca cada 10 segundos igual que el resto del dashboard.

MODULO (render_modulo, app.py ~línea 674):
  - Parámetro 'costura' o 'acabados'. Estado destino = EN COSTURA o EN ACABADOS.
  - Buscador por código o color (key f'buscar_{modo}').
  - Filtro por estado actual (key f'estado_filtro_{modo}').
  - Lista de todos los códigos con sus colores como checkboxes
      (key f'mod_{modo}_{style} — {color}').
  - Botón 'Guardar selección' type='primary' (key f'guardar_{modo}').
      Actualiza db.actualizar_seguimiento(style,color,status=estado_destino) por cada marcada.
  - Vista previa de selección en un expander.

--------------------------------------------------------------------------------
8. CSS / DISEÑO (inyectar_css)
--------------------------------------------------------------------------------
ESTILO: "Glassmorphism suave" sobre fondo neutro blanco/negro.
  - Paleta neutra (blanco/negro/grises). SIN violeta en el diseño.
  - GRÁFICOS e indicadores usan colores libres (CHART_COLORS, plotly_dark).
Variables CSS (en :root):
  --sms-bg-top #0B0B0F, --sms-bg-bottom #16161D
  --sms-glass rgba(255,255,255,0.05), --sms-glass-strong rgba(255,255,255,0.09)
  --sms-glass-border rgba(255,255,255,0.12)
  --sms-text #F5F5F7, --sms-muted rgba(255,255,255,0.55)
  --sms-border rgba(255,255,255,0.10), --sms-black #000, --sms-white #FFF

ELEMENTOS CON ESTILO DE CRISTAL (backdrop-filter blur):
  - Sidebar, métricas (stMetrics), botones, tabs (píldoras), tabla, inputs/selectbox,
    file_uploader, dropdown, expander/paneles, tabla resumen (.sms-resumen).

TABLA RESUMEN (.sms-resumen):
  - Tabla HTML custom (no st.dataframe) con estilo glassmorphism.
  - Fondo: var(--sms-glass) con blur 12px. Borde: var(--sms-glass-border), radio 16px.
  - Headers: fondo rgba(255,255,255,0.09), texto muted uppercase.
  - Celdas: texto var(--sms-text), hover sutil rgba(255,255,255,0.04).
  - Fila TOTAL: fondo rgba(255,255,255,0.07), bold, borde superior.
  - Puntos de color (.sms-dot): círculos de 10px con color del estado (CHART_COLORS).
  - Números: alineados derecha, tabular-nums, formateados con separadores de miles.

BOTONES PRIMARIOS (type='primary'):
  - Fondo gradiente BLANCO (#FFFFFF -> #D9D9E3), texto NEGRO #000000 !important.
  - Se forzó el texto negro también en hijos (p, span, *) para garantizar legibilidad:
      button[kind="primary"] *,  button[data-testid="stBaseButton-primary"] *
  - Selectores usados (alta especificidad para ganar a emotion cache de Streamlit):
      button[kind="primary"], div.stButton > button[data-testid="stBaseButton-primary"],
      div[data-testid="stForm"] button[data-testid="stBaseButton-primary"],
      button[data-testid="stBaseButton-primary"],
      div[data-testid="stMainBlockContainer"] button[kind="primary"],
      div[data-testid="stHorizontalBlock"] button[kind="primary"]

TABS: Píldoras (border-radius 999px) de cristal. Activo = gradiente blanco con textos negros.
  Selectores de alta especificidad (para ganar a emotion cache):
      div[data-testid="stTabs"] [role="tablist"] [data-testid="stTab"]
  Se ocultaron pseudo-elementos (::before/::after) y .react-aria-SelectionIndicator.

HEADER de Streamlit (stHeader): flotaba y tapaba botones. Se arregló:
  - position: fixed, min-height 48px, z-index 1000, fondo transparente.
  - Botones del header con estilo cristal.
  - .block-container con padding-top 5rem para que el contenido no quede tapado.

RESPONSIVE (media queries al final del CSS):
  >=1200px        -> contenedor amplio.
  992-1199px      -> contenedor 1200px.
  768-991px (TABLET)-> completamente táctil: botones 44px, métricas apilables,
                        tabs con scroll, checkboxes táctiles, sidebar 280px.
  <768px (MÓVIL)  -> todo a ancho completo, sidebar 260px.
  Clave: .block-container [data-testid="column"] { min-width:0; word-break:break-word }

--------------------------------------------------------------------------------
9. CAPA DE DATOS (datos.py)
--------------------------------------------------------------------------------
ESTADOS normalizados:
  TELA X ING 3-SET, EN LAVANDERÍA, EN CORTE, EN COSTURA, EN ACABADOS, ENCAJADO, (sin estado)

TALLAS (14): one size, 4-8y, 0-6m, 6-12m, 3-6m, 6-9m, 9-12m, 2y, 3y, 4Y, 5y, 6Y, 8y, 10y

Funciones principales:
  obtener_hojas(file_path)     -> lista de nombres de hojas del Excel.
  extractar_hoja(file, sheet)  -> filas: listado de dicts por Style+Color.
      - Detecta fila de headers dinámicamente (_find_header_row donte ve 'STYLE' + 'PO'/'OP').
      - Detecta columnas (_detect_layout): style, po, tendido, name, tela, color, status,
        ingreso, obs, y dos bloques 'total' (PEDIDO y PROGRAMADO).
      - Localiza bloques de tallas por las palabras PEDIDO / PROGRAMADO.
      - Normaliza estados (_normalizar_estado) y acentos corruptos.
  Campos por fila: style, po, name, tela, color, pedido[], prog[], pedido_total,
      prog_total, status, ingreso, obs.

--------------------------------------------------------------------------------
10. PERSISTENCIA (db.py)
--------------------------------------------------------------------------------
Base: seguimiento.db (SQLite). Tabla: prendas.
Clave primaria: id = f'{style}@@{color}'.
Campos: id, style, po, name, tela, color, tallas_pedido (JSON), total_pedido,
        tallas_prog (JSON), total_prog, status, ingreso, observaciones, fecha_importacion.

Funciones:
  init_db()                    -> crea tabla si no existe.
  reemplazar_datos(filas)      -> borra todo y reinserta (usado al importar).
  actualizar_seguimiento(style, color, status=None, observaciones=None,
                          total_prog=None, tallas_prog=None) -> update parcial.
  obtener_todos()              -> todas las filas (con pedido[]/prog[] deserializados).
  obtener_por_id(style, color) -> una variante.
  conteo_por_estado()          -> dict de conteos por estado.

--------------------------------------------------------------------------------
11. REPORTES (reporte.py)
--------------------------------------------------------------------------------
Usado por la pestaña Exportar:
  reporte.exportar_excel(datos_db) -> bytes del .xlsx (RESUMEN GENERAL + DETALLE POR ESTILO).
  reporte.exportar_pdf(datos_db)   -> bytes del .pdf (resumen + 2 gráficos).

Standalone (NO los usa la app, generan archivos físicos):
  generar_reporte.py -> REPORTE SMS-SUMMER27.xlsx
  generar_pdf.py     -> REPORTE SMS-SUMMER27.pdf

--------------------------------------------------------------------------------
12. DEPENDENCIAS (requirements.txt)
--------------------------------------------------------------------------------
streamlit, openpyxl, pandas, plotly, matplotlib, reportlab.
Config (.streamlit/config.toml): base dark, primaryColor #7C5CFC, bg #0A0A0F,
  secondaryBg #12121A, texto #E8E8ED, servidor 0.0.0.0:8501, headless.

--------------------------------------------------------------------------------
13. ESTADO ACTUAL Y VERIFICACIÓN
--------------------------------------------------------------------------------
- AppTest (prueba headless de Streamlit): 0 excepciones.
- Servidor: /_stcore/health = 200 y página '/' = 200.
- Flujo módulo Costura verificado: marcar check + Guardar cambia estado a EN COSTURA
  (se probó y luego se restauró el dato de prueba a 'TELA X ING 3-SET').
- El botón primario 'Guardar selección' se ve con texto NEGRO sobre fondo blanco.

DATOS EN seguimiento.db AL MOMENTO DE ESTE DOC: importados del Excel (176 variantes
de 76 estilos). A la(s) prenda(s) usadas en pruebas de Costura se les restauró su
estado original (B3027U27 / 'Foxglove Scallops' quedó en 'TELA X ING 3-SET').

--------------------------------------------------------------------------------
14. CAMBIO PLANIFICADO: MULTI-HOJA / MULTI-PROYECTO (Pendiente de implementar)
--------------------------------------------------------------------------------
OBJETIVO: Cada hoja del Excel = un proyecto independiente (ej. SMS-01432, WINTER).
Se pueden importar varias hojas y el dashboard tiene un selector para ver una u otra.

DECISIONES DEL USUARIO:
  - Importación: Reemplaza todo (pero por hoja, no global).
  - Filtro de hojas: Elegir una por una con checkboxes.
  - Selector de proyecto: Dentro del Dashboard (selectbox).
  - Modo solo lectura (?solo=1): Selector + dashboard.

PLAN DE IMPLEMENTACIÓN:

  Fase 1: db.py — Schema y capa de datos
    - Schema: Agregar columna 'hoja TEXT NOT NULL'. PK nueva: hoja@@style@@color
    - _id(): Cambia a _id(hoja, style, color)
    - reemplazar_datos(hoja, filas): DELETE WHERE hoja = ? (no borra otras hojas)
    - obtener_todos(hoja=None): Filtro opcional por hoja
    - actualizar_seguimiento(hoja, style, color, ...): Usa la PK nueva
    - Nueva obtener_hojas_disponibles(): Lista hojas distintas en la DB
    - Migración: Si DB vieja no tiene columna 'hoja', se dropea y recrea

  Fase 2: app.py — Pestaña Importar
    - Reemplazar selectbox por checkboxes (una por cada hoja del Excel)
    - Botón "Importar datos seleccionados" importa solo las marcadas
    - Cada hoja se importa por separado con reemplazar_datos(hoja, filas)

  Fase 3: app.py — Selector de proyecto en Dashboard
    - Selectbox con las hojas disponibles en la DB
    - st.session_state.hoja_actual = la hoja seleccionada
    - Se pasa como filtro a render_dashboard(hoja=...) y titulo_dashboard(hoja=...)

  Fase 4: app.py — Filtro en todas las pestañas
    - Seguimiento: db.obtener_todos(hoja=actual)
    - Costura/Acabados: db.obtener_todos(hoja=actual) + hoja en actualizar_seguimiento
    - Exportar: Exporta solo la hoja seleccionada

  Fase 5: app.py — Modo solo lectura (?solo=1)
    - Selectbox de proyecto arriba del dashboard
    - Soporte opcional: ?solo=1&hoja=SMS-01432

  Fase 6: CONTEXTO_PROYECTO.txt — Documentar todo el nuevo flujo

  CASOS EDGE:
    - DB vieja sin columna 'hoja' → migración automática
    - Hoja sin filas válidas → error al importar, no se inserta nada
    - Re-importar una hoja → borra solo esa, preserva las demás
    - Sin datos → selector vacío, muestra info

  ANTES DE EMPEZAR:
    - Inicializar git y hacer commit inicial (usuario ya instaló git)
    - Si algo sale mal → git checkout para retroceder

--------------------------------------------------------------------------------
15. OTROS PENDIENTES
--------------------------------------------------------------------------------
- La métrica 'Avance' muestra texto 'En Progreso' (no porcentaje). Si se necesita
  el % de nuevo o una fórmula distinta, ajustar en render_dashboard (app.py).
- Hay muchos módulos/hojas en el Excel fuente (Cliente SMS, WIP-MISHA, TOP MAMA OWL,
  PEFIT, FRANCIS, etc.). El título usa el PO para derivar el cliente.
- Concurrencia: ver sección 5.

================================================================================
FIN DEL DOCUMENTO
================================================================================